{
  "name": "lesson-3-intro-html",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode dev",
    "build": "vite build",
    "preview": "vite preview",
    "cy:open": "yarn dev & cypress open --e2e --config-file=cypress.config.js",
    "server:simple": "simple-server dist 3000",
    "server:json": "json-server --watch db.json --port 3003 --static dist -delay 1000",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/ui": "^0.25.1",
    "autoprefixer": "^10.4.12",
    "cypress": "^10.10.0",
    "json-server": "^0.17.1",
    "postcss": "^8.4.17",
    "tailwindcss": "^3.1.8",
    "vite": "^3.1.0",
    "vite-plugin-remove-console": "^1.3.0",
    "vitest": "^0.25.1"
  },
  "dependencies": {
    "carbon-web-components": "^1.21.0",
    "prettier": "^2.7.1"
  },
  "prettier": {
    "printWidth": 120,
    "singleQuote": true
  }
}
